PORT=8080
NODE_ENV=development

# Required for cookies (admin sessions + future features)
# Use a long random string (example: openssl rand -hex 32)
COOKIE_SECRET=

DATABASE_URL=postgres://passhroom:passhroom@localhost:5432/passhroom
REDIS_URL=redis://localhost:6379

PASSHROOM_PUBLIC_BASE_URL=http://localhost:8080

# Admin dashboard
ADMIN_ENABLED=true
ADMIN_LOGIN_TTL_MINUTES=10
ADMIN_SESSION_TTL_HOURS=12
# Optional extra guard for /admin routes
# ADMIN_REQUIRE_HEADER_NAME=X-Admin-Key
# ADMIN_REQUIRE_HEADER_VALUE=

SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
SMTP_FROM_NAME=Passhroom

TOKEN_TTL_MINUTES=10
CODE_TTL_MINUTES=5
REQUIRE_HTTPS=false
LOG_REDACT_PII=true
RESEND_COOLDOWN_SECONDS=60
MAX_MAGIC_ATTEMPTS=5

RL_IP_PER_MINUTE=10
RL_EMAIL_PER_MINUTE=3
RL_EMAIL_PER_HOUR=10
RL_CLIENT_PER_MINUTE=20

RATE_LIMIT_BACKEND=auto
