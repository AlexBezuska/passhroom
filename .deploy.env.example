# Local (uncommitted) config for scripts/deploy-host/*.sh
#
# Copy:
#   cp .deploy.env.example .deploy.env
# Then edit .deploy.env with your own values.
#
# This file is safe to commit; .deploy.env is NOT.

# SSH target
DEPLOY_HOST=
DEPLOY_USER=
DEPLOY_SSH_PORT=22

# Passhroom deployment
PASSHROOM_DOMAIN=passhroom.example.com
PASSHROOM_UPSTREAM=http://127.0.0.1:18080

# If a script uses SERVER/REMOTE_DIR instead of DEPLOY_HOST/USER
# SERVER="user@host"
# REMOTE_DIR="/path/to/passhroom"

# Server directory layout
DEPLOY_ROOT=
PASSHROOM_DIR=
WEBSERVER_DIR=
NGINX_CONFD_DIR=
SITE_ROOT_DIR=
SITE_DIR=

# Container/network names (optional)
DOCKER_NETWORK_WEB_NAME=web
NGINX_CONTAINER_NAME=nginx_web
CERTBOT_CONTAINER_NAME=certbot

# Certbot contact email (used by setup-domain-ssl.sh)
EMAIL=
